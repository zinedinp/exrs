//! **Prototype sketch, not wired into `decode_lossy_dct_group`'s dispatch and
//! not ready to ship.**
//!
//! `#[allow(dead_code)]` because nothing in the crate calls this yet.

#[allow(dead_code)]
pub mod neon;
